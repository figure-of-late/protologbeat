- key: protologbeat
  title: protologbeat
  description:
  fields:
    - name: counter
      type: long
      required: true
      description: >
        Go program which accepts local log messages via TCP/UDP and ships them to a libbeat-supported output
